<%= form_for(@playlist) do |f| %>
  <h4>新しいプレイリストを作成する</h4>
  <%= render 'shared/error_messages', object:f.object %>
  <div class="field">
    <%= f.text_area :playlist_name, placeholder: "ここにプレイリスト名を入力" %>
  </div>
  <%= f.submit "投稿する", class: "btn btn-primary" %>
<% end %>